# Max Messenger Bot Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create a standalone Max messenger bot for accepting service requests, notifying users about status changes/verification reminders, and admin broadcast messaging.

**Architecture:** Separate Node.js process (`bot/index.ts`) using `@maxhub/max-bot-api`. Shares SQLite DB via Prisma. New `MaxUser` model links Max user IDs to site accounts. Bot runs as a second Docker container alongside the Next.js app.

**Tech Stack:** `@maxhub/max-bot-api`, Prisma, TypeScript, node-cron

---

### Task 1: Prisma Schema — MaxUser + MaxLinkCode models

**Files:**
- Modify: `prisma/schema.prisma`

**Step 1: Add MaxUser and MaxLinkCode models to schema**

Add at the end of `prisma/schema.prisma`:

```prisma
model MaxUser {
  id        Int      @id @default(autoincrement())
  maxUserId Int      @unique
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@index([userId])
}

model MaxLinkCode {
  id        Int      @id @default(autoincrement())
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  code      String
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())

  @@index([userId])
  @@index([code])
}
```

Also add reverse relations to the `User` model:

```prisma
maxAccounts    MaxUser[]
maxLinkCodes   MaxLinkCode[]
```

**Step 2: Generate migration**

Run: `npx prisma migrate dev --name add-max-user`

**Step 3: Commit**

```bash
git add prisma/
git commit -m "feat(bot): add MaxUser and MaxLinkCode models"
```

---

### Task 2: API — Generate link code endpoint

**Files:**
- Create: `src/app/api/user/max-link/route.ts`

**Step 1: Create the endpoint**

```typescript
import { JWT_SECRET } from "@/lib/jwt";
import { prisma } from "@/lib/prisma";
import { jwtVerify } from "jose";
import { NextRequest, NextResponse } from "next/server";

export const dynamic = "force-dynamic";

export async function POST(req: NextRequest) {
  const token = req.cookies.get("auth-token")?.value;
  if (!token) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  let userId: number;
  try {
    const { payload } = await jwtVerify(token, JWT_SECRET);
    userId = payload.userId as number;
  } catch {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  // Invalidate old codes
  await prisma.maxLinkCode.updateMany({
    where: { userId, used: false },
    data: { used: true },
  });

  // Generate 6-digit code
  const code = String(Math.floor(100000 + Math.random() * 900000));
  const expiresAt = new Date(Date.now() + 5 * 60 * 1000); // 5 min

  await prisma.maxLinkCode.create({
    data: { userId, code, expiresAt },
  });

  return NextResponse.json({ code, expiresAt });
}
```

**Step 2: Commit**

```bash
git add src/app/api/user/max-link/
git commit -m "feat(bot): add API endpoint to generate Max link codes"
```

---

### Task 3: Dashboard UI — "Link Max Account" button on profile page

**Files:**
- Modify: `src/app/dashboard/profile/page.tsx`

**Step 1: Add Max linking section to profile page**

Add a section after existing profile content. Shows a button "Привязать Max". On click, calls `POST /api/user/max-link`, displays the 6-digit code with a 5-minute countdown. If user already has a linked Max account (check via new `GET /api/user/max-link`), show "Привязан" with an unlink button.

Add GET handler to `src/app/api/user/max-link/route.ts`:

```typescript
export async function GET(req: NextRequest) {
  const token = req.cookies.get("auth-token")?.value;
  if (!token) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  let userId: number;
  try {
    const { payload } = await jwtVerify(token, JWT_SECRET);
    userId = payload.userId as number;
  } catch {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const maxUser = await prisma.maxUser.findFirst({ where: { userId } });
  return NextResponse.json({ linked: !!maxUser });
}

export async function DELETE(req: NextRequest) {
  const token = req.cookies.get("auth-token")?.value;
  if (!token) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  let userId: number;
  try {
    const { payload } = await jwtVerify(token, JWT_SECRET);
    userId = payload.userId as number;
  } catch {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  await prisma.maxUser.deleteMany({ where: { userId } });
  return NextResponse.json({ success: true });
}
```

**Step 2: Commit**

```bash
git add src/app/api/user/max-link/ src/app/dashboard/profile/
git commit -m "feat(bot): add Max account linking UI on profile page"
```

---

### Task 4: Bot scaffold — project setup and main menu

**Files:**
- Create: `bot/package.json`
- Create: `bot/tsconfig.json`
- Create: `bot/src/index.ts`
- Create: `bot/src/keyboards.ts`

**Step 1: Create bot/package.json**

```json
{
  "name": "csm-max-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@maxhub/max-bot-api": "latest",
    "@prisma/client": "^6.2.1",
    "node-cron": "^3.0.3",
    "dotenv": "^17.2.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
```

**Step 2: Create bot/tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src"]
}
```

**Step 3: Create bot/src/keyboards.ts**

```typescript
import { Keyboard } from "@maxhub/max-bot-api";

export function mainMenu() {
  return Keyboard.inlineKeyboard([
    [Keyboard.button.callback("Подать заявку", "new_request")],
    [Keyboard.button.callback("Мои заявки", "my_requests")],
    [Keyboard.button.callback("Привязать аккаунт", "link_account")],
    [
      Keyboard.button.callback("О компании", "about"),
      Keyboard.button.callback("Контакты", "contacts"),
    ],
  ]);
}

export function servicesMenu() {
  return Keyboard.inlineKeyboard([
    [Keyboard.button.callback("Поверка СИ", "service:Поверка СИ")],
    [Keyboard.button.callback("Калибровка", "service:Калибровка")],
    [Keyboard.button.callback("Сертификация", "service:Сертификация")],
    [Keyboard.button.callback("Аттестация ИЛ", "service:Аттестация")],
    [Keyboard.button.callback("Отмена", "cancel")],
  ]);
}

export function confirmMenu() {
  return Keyboard.inlineKeyboard([
    [
      Keyboard.button.callback("Отправить", "confirm_request", { intent: "positive" }),
      Keyboard.button.callback("Отмена", "cancel", { intent: "negative" }),
    ],
  ]);
}

export function backMenu() {
  return Keyboard.inlineKeyboard([
    [Keyboard.button.callback("Главное меню", "main_menu")],
  ]);
}
```

**Step 4: Create bot/src/index.ts (scaffold with main menu)**

```typescript
import "dotenv/config";
import { Bot, Context } from "@maxhub/max-bot-api";
import { PrismaClient } from "@prisma/client";
import { mainMenu } from "./keyboards";

const prisma = new PrismaClient();
const bot = new Bot(process.env.MAX_BOT_TOKEN!);

// Show main menu on start
bot.on("bot_started", (ctx) => {
  ctx.reply("Добро пожаловать в ЦСМ «Стандарт»!\n\nВыберите действие:", {
    attachments: [mainMenu()],
  });
});

bot.action("main_menu", (ctx) => {
  ctx.reply("Выберите действие:", { attachments: [mainMenu()] });
});

bot.start().then(() => console.log("Max bot started"));

export { bot, prisma };
```

**Step 5: Commit**

```bash
git add bot/
git commit -m "feat(bot): scaffold Max bot with main menu keyboard"
```

---

### Task 5: Bot — Request submission flow

**Files:**
- Create: `bot/src/sessions.ts`
- Create: `bot/src/handlers/request.ts`
- Modify: `bot/src/index.ts`

**Step 1: Create bot/src/sessions.ts (in-memory session store)**

```typescript
interface RequestSession {
  step: "awaiting_service" | "awaiting_contact" | "awaiting_confirm";
  service?: string;
  phone?: string;
  email?: string;
}

const sessions = new Map<number, RequestSession>();

export function getSession(userId: number) {
  return sessions.get(userId);
}

export function setSession(userId: number, data: RequestSession) {
  sessions.set(userId, data);
}

export function clearSession(userId: number) {
  sessions.delete(userId);
}
```

**Step 2: Create bot/src/handlers/request.ts**

```typescript
import { Context } from "@maxhub/max-bot-api";
import { PrismaClient } from "@prisma/client";
import { servicesMenu, confirmMenu, backMenu } from "../keyboards";
import { getSession, setSession, clearSession } from "../sessions";

const prisma = new PrismaClient();

export function registerRequestHandlers(bot: any) {
  // Start request flow
  bot.action("new_request", (ctx: Context) => {
    const userId = ctx.user?.user_id;
    if (!userId) return;
    setSession(userId, { step: "awaiting_service" });
    ctx.reply("Выберите услугу:", { attachments: [servicesMenu()] });
  });

  // Service selected
  bot.action(/^service:(.+)$/, (ctx: Context) => {
    const userId = ctx.user?.user_id;
    if (!userId) return;
    const session = getSession(userId);
    if (!session) return;

    const match = ctx.callbackData?.match(/^service:(.+)$/);
    const service = match?.[1] || "";

    setSession(userId, { ...session, step: "awaiting_contact", service });
    ctx.reply(
      `Услуга: ${service}\n\nВведите ваш телефон и email через пробел.\nПример: +79001234567 mail@example.com`
    );
  });

  // Cancel
  bot.action("cancel", (ctx: Context) => {
    const userId = ctx.user?.user_id;
    if (userId) clearSession(userId);
    ctx.reply("Заявка отменена.", { attachments: [backMenu()] });
  });
}
```

**Step 3: Add message handler for contact input in request.ts**

```typescript
export function handleMessage(ctx: Context) {
  const userId = ctx.user?.user_id;
  if (!userId) return false;
  const session = getSession(userId);
  if (!session) return false;

  const text = ctx.message?.body?.text?.trim();
  if (!text) return false;

  if (session.step === "awaiting_contact") {
    const parts = text.split(/\s+/);
    const phone = parts.find((p) => /^\+?\d{10,15}$/.test(p));
    const email = parts.find((p) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(p));

    if (!phone || !email) {
      ctx.reply("Не удалось распознать телефон или email. Попробуйте снова.\nПример: +79001234567 mail@example.com");
      return true;
    }

    setSession(userId, { ...session, step: "awaiting_confirm", phone, email });
    ctx.reply(
      `Проверьте данные:\n\nУслуга: ${session.service}\nТелефон: ${phone}\nEmail: ${email}`,
      { attachments: [confirmMenu()] }
    );
    return true;
  }

  return false;
}
```

**Step 4: Add confirm handler that saves to DB**

```typescript
export function registerConfirmHandler(bot: any) {
  bot.action("confirm_request", async (ctx: Context) => {
    const userId = ctx.user?.user_id;
    if (!userId) return;
    const session = getSession(userId);
    if (!session || session.step !== "awaiting_confirm") return;

    // Check if Max user is linked
    const maxUser = await prisma.maxUser.findUnique({ where: { maxUserId: userId } });

    await prisma.request.create({
      data: {
        name: ctx.user?.name || "Max User",
        phone: session.phone!,
        email: session.email!,
        service: session.service!,
        status: "new",
        userId: maxUser?.userId || null,
        items: {
          create: [{ service: session.service! }],
        },
      },
    });

    clearSession(userId);
    ctx.reply("Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время.", {
      attachments: [backMenu()],
    });
  });
}
```

**Step 5: Wire handlers into index.ts**

```typescript
import { registerRequestHandlers, registerConfirmHandler, handleMessage } from "./handlers/request";

registerRequestHandlers(bot);
registerConfirmHandler(bot);

bot.on("message_created", (ctx) => {
  if (!handleMessage(ctx)) {
    // Unknown text — show main menu
    ctx.reply("Выберите действие:", { attachments: [mainMenu()] });
  }
});
```

**Step 6: Commit**

```bash
git add bot/
git commit -m "feat(bot): request submission flow with button-driven UI"
```

---

### Task 6: Bot — Account linking handler

**Files:**
- Create: `bot/src/handlers/link.ts`
- Modify: `bot/src/index.ts`
- Modify: `bot/src/sessions.ts`

**Step 1: Add link session type to sessions.ts**

Add `LinkSession` type and separate map, or extend existing session with a `type` field. Simplest: add `awaitingLinkCode` boolean map.

```typescript
const linkSessions = new Set<number>();

export function setAwaitingLink(userId: number) { linkSessions.add(userId); }
export function isAwaitingLink(userId: number) { return linkSessions.has(userId); }
export function clearAwaitingLink(userId: number) { linkSessions.delete(userId); }
```

**Step 2: Create bot/src/handlers/link.ts**

```typescript
import { Context } from "@maxhub/max-bot-api";
import { PrismaClient } from "@prisma/client";
import { backMenu } from "../keyboards";
import { setAwaitingLink, isAwaitingLink, clearAwaitingLink } from "../sessions";

const prisma = new PrismaClient();

export function registerLinkHandlers(bot: any) {
  bot.action("link_account", async (ctx: Context) => {
    const maxUserId = ctx.user?.user_id;
    if (!maxUserId) return;

    const existing = await prisma.maxUser.findUnique({ where: { maxUserId } });
    if (existing) {
      ctx.reply("Ваш аккаунт уже привязан.", { attachments: [backMenu()] });
      return;
    }

    setAwaitingLink(maxUserId);
    ctx.reply("Введите 6-значный код из личного кабинета на сайте (Профиль -> Привязать Max):");
  });
}

export function handleLinkMessage(ctx: Context): boolean {
  const maxUserId = ctx.user?.user_id;
  if (!maxUserId || !isAwaitingLink(maxUserId)) return false;

  const text = ctx.message?.body?.text?.trim();
  if (!text || !/^\d{6}$/.test(text)) {
    ctx.reply("Введите 6-значный код:");
    return true;
  }

  (async () => {
    const linkCode = await prisma.maxLinkCode.findFirst({
      where: { code: text, used: false, expiresAt: { gte: new Date() } },
    });

    if (!linkCode) {
      ctx.reply("Код неверный или истёк. Попробуйте получить новый код на сайте.", {
        attachments: [backMenu()],
      });
      clearAwaitingLink(maxUserId);
      return;
    }

    await prisma.maxLinkCode.update({ where: { id: linkCode.id }, data: { used: true } });
    await prisma.maxUser.create({ data: { maxUserId, userId: linkCode.userId } });

    clearAwaitingLink(maxUserId);
    ctx.reply("Аккаунт успешно привязан! Теперь вы будете получать уведомления.", {
      attachments: [backMenu()],
    });
  })();

  return true;
}
```

**Step 3: Wire into index.ts message handler**

```typescript
import { registerLinkHandlers, handleLinkMessage } from "./handlers/link";

registerLinkHandlers(bot);

// Update message_created handler:
bot.on("message_created", (ctx) => {
  if (handleLinkMessage(ctx)) return;
  if (handleMessage(ctx)) return;
  ctx.reply("Выберите действие:", { attachments: [mainMenu()] });
});
```

**Step 4: Commit**

```bash
git add bot/
git commit -m "feat(bot): account linking via 6-digit code"
```

---

### Task 7: Bot — My requests handler

**Files:**
- Create: `bot/src/handlers/requests-list.ts`
- Modify: `bot/src/index.ts`

**Step 1: Create handler**

```typescript
import { Context } from "@maxhub/max-bot-api";
import { PrismaClient } from "@prisma/client";
import { backMenu } from "../keyboards";

const prisma = new PrismaClient();

const STATUS_LABELS: Record<string, string> = {
  new: "Новая",
  in_progress: "В работе",
  pending_payment: "Ожидает оплаты",
  review: "На проверке",
  done: "Выполнена",
  cancelled: "Отменена",
};

export function registerRequestsListHandlers(bot: any) {
  bot.action("my_requests", async (ctx: Context) => {
    const maxUserId = ctx.user?.user_id;
    if (!maxUserId) return;

    const maxUser = await prisma.maxUser.findUnique({ where: { maxUserId } });
    if (!maxUser) {
      ctx.reply("Сначала привяжите аккаунт, чтобы видеть заявки.", {
        attachments: [backMenu()],
      });
      return;
    }

    const requests = await prisma.request.findMany({
      where: { userId: maxUser.userId },
      orderBy: { createdAt: "desc" },
      take: 10,
    });

    if (requests.length === 0) {
      ctx.reply("У вас пока нет заявок.", { attachments: [backMenu()] });
      return;
    }

    const lines = requests.map((r) => {
      const date = r.createdAt.toLocaleDateString("ru-RU");
      const status = STATUS_LABELS[r.status] || r.status;
      return `#${r.id} | ${r.service} | ${status} | ${date}`;
    });

    ctx.reply(`Ваши последние заявки:\n\n${lines.join("\n")}`, {
      attachments: [backMenu()],
      format: "markdown",
    });
  });
}
```

**Step 2: Wire into index.ts**

**Step 3: Commit**

```bash
git add bot/
git commit -m "feat(bot): my requests list handler"
```

---

### Task 8: Bot — About & Contacts handlers

**Files:**
- Create: `bot/src/handlers/info.ts`
- Modify: `bot/src/index.ts`

**Step 1: Create handler using SiteSettings from DB**

```typescript
import { Context } from "@maxhub/max-bot-api";
import { PrismaClient } from "@prisma/client";
import { backMenu } from "../keyboards";

const prisma = new PrismaClient();

export function registerInfoHandlers(bot: any) {
  bot.action("about", (ctx: Context) => {
    ctx.reply(
      "**ЦСМ «Стандарт»**\n\n" +
      "Центр сертификации и метрологии.\n" +
      "Оказываем услуги поверки, калибровки, сертификации и аттестации.\n\n" +
      "Сайт: csm-center.ru",
      { attachments: [backMenu()], format: "markdown" }
    );
  });

  bot.action("contacts", async (ctx: Context) => {
    const settings = await prisma.setting.findMany({
      where: { key: { in: ["companyPhone", "companyEmail", "companyAddress"] } },
    });
    const get = (key: string) => settings.find((s) => s.key === key)?.value || "";

    const phone = get("companyPhone") || "+7 (XXX) XXX-XX-XX";
    const email = get("companyEmail") || "zakaz@csm-center.ru";
    const address = get("companyAddress") || "";

    const lines = [
      "**Контакты**",
      "",
      `Телефон: ${phone}`,
      `Email: ${email}`,
      address ? `Адрес: ${address}` : "",
      "",
      "Сайт: csm-center.ru",
    ];

    ctx.reply(lines.filter(Boolean).join("\n"), {
      attachments: [backMenu()],
      format: "markdown",
    });
  });
}
```

**Step 2: Commit**

```bash
git add bot/
git commit -m "feat(bot): about and contacts info handlers"
```

---

### Task 9: Status change notifications to Max users

**Files:**
- Create: `bot/src/notify.ts`
- Modify: `src/app/api/admin/[id]/route.ts`

**Step 1: Create bot/src/notify.ts — HTTP helper for sending messages via Max API**

Since the bot runs as a separate process, the Next.js app needs a way to trigger messages. Simplest: Next.js calls the Max API directly (same as existing `src/lib/max.ts` pattern).

Create `src/lib/maxUserNotify.ts`:

```typescript
import { prisma } from "@/lib/prisma";

const MAX_API_BASE = "https://platform-api.max.ru";

const STATUS_LABELS: Record<string, string> = {
  new: "Новая",
  in_progress: "В работе",
  pending_payment: "Ожидает оплаты",
  review: "На проверке",
  done: "Выполнена",
  cancelled: "Отменена",
};

export async function notifyMaxUserStatusChange(params: {
  userId: number;
  requestId: number;
  status: string;
  service: string;
}) {
  const token = process.env.MAX_BOT_TOKEN;
  if (!token) return;

  const maxUser = await prisma.maxUser.findFirst({
    where: { userId: params.userId },
  });
  if (!maxUser) return;

  const statusLabel = STATUS_LABELS[params.status] || params.status;
  const text = `Заявка #${params.requestId} (${params.service})\n\nСтатус изменён: **${statusLabel}**`;

  try {
    await fetch(`${MAX_API_BASE}/messages?user_id=${maxUser.maxUserId}`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: token,
      },
      body: JSON.stringify({ text, format: "markdown", notify: true }),
    });
  } catch (error) {
    console.error("Max user notification error:", error);
  }
}
```

**Step 2: Call from admin PATCH route**

In `src/app/api/admin/[id]/route.ts`, after the email notification block (~line 114), add:

```typescript
import { notifyMaxUserStatusChange } from "@/lib/maxUserNotify";

// After status change email block:
if (updateData.status && updateData.status !== current.status && updated.userId) {
  notifyMaxUserStatusChange({
    userId: updated.userId,
    requestId: updated.id,
    status: updateData.status,
    service: updated.service,
  }).catch(console.error);
}
```

**Step 3: Commit**

```bash
git add src/lib/maxUserNotify.ts src/app/api/admin/
git commit -m "feat(bot): notify Max users on request status change"
```

---

### Task 10: Verification reminder cron in bot

**Files:**
- Create: `bot/src/cron.ts`
- Modify: `bot/src/index.ts`

**Step 1: Create bot/src/cron.ts**

```typescript
import cron from "node-cron";
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();
const MAX_API_BASE = "https://platform-api.max.ru";

export function startCronJobs() {
  // Run daily at 09:00 Moscow time
  cron.schedule("0 9 * * *", async () => {
    console.log("Running verification reminder check...");
    const token = process.env.MAX_BOT_TOKEN;
    if (!token) return;

    const now = new Date();
    const in14Days = new Date(now.getTime() + 14 * 24 * 60 * 60 * 1000);

    const equipment = await prisma.equipment.findMany({
      where: {
        notified: false,
        ignored: false,
        nextVerification: { lte: in14Days, gte: now },
      },
      include: { user: true },
    });

    if (equipment.length === 0) return;

    // Group by user
    const byUser = new Map<number, typeof equipment>();
    for (const eq of equipment) {
      if (!eq.userId) continue;
      const list = byUser.get(eq.userId) || [];
      list.push(eq);
      byUser.set(eq.userId, list);
    }

    for (const [userId, items] of byUser) {
      const maxUser = await prisma.maxUser.findFirst({ where: { userId } });
      if (!maxUser) continue;

      const lines = items.map((eq) => {
        const date = eq.nextVerification
          ? eq.nextVerification.toLocaleDateString("ru-RU")
          : "—";
        return `- ${eq.name}${eq.serialNumber ? ` (${eq.serialNumber})` : ""} — до ${date}`;
      });

      const text = `**Напоминание о поверке**\n\nСледующие СИ требуют поверки в ближайшие 14 дней:\n\n${lines.join("\n")}`;

      try {
        await fetch(`${MAX_API_BASE}/messages?user_id=${maxUser.maxUserId}`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: token,
          },
          body: JSON.stringify({ text, format: "markdown", notify: true }),
        });
      } catch (error) {
        console.error(`Max reminder error for user ${userId}:`, error);
      }
    }

    console.log(`Verification reminders sent to ${byUser.size} Max users`);
  });
}
```

**Step 2: Call from index.ts**

```typescript
import { startCronJobs } from "./cron";

// After bot.start():
startCronJobs();
```

**Step 3: Commit**

```bash
git add bot/
git commit -m "feat(bot): daily verification reminder cron for Max users"
```

---

### Task 11: Admin broadcast API + handler

**Files:**
- Create: `src/app/api/admin/broadcast/route.ts`

**Step 1: Create broadcast endpoint**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { verifyAdminPassword } from "@/lib/adminAuth";

export const dynamic = "force-dynamic";

const MAX_API_BASE = "https://platform-api.max.ru";

export async function POST(req: NextRequest) {
  const headerPassword = req.headers.get("x-admin-password");
  if (!headerPassword || !(await verifyAdminPassword(headerPassword))) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const { message } = await req.json();
  if (!message || typeof message !== "string" || message.length > 2000) {
    return NextResponse.json({ error: "Invalid message" }, { status: 400 });
  }

  const token = process.env.MAX_BOT_TOKEN;
  if (!token) {
    return NextResponse.json({ error: "Max bot not configured" }, { status: 500 });
  }

  const maxUsers = await prisma.maxUser.findMany();
  let sent = 0;

  for (const mu of maxUsers) {
    try {
      await fetch(`${MAX_API_BASE}/messages?user_id=${mu.maxUserId}`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: token,
        },
        body: JSON.stringify({ text: message, format: "markdown", notify: true }),
      });
      sent++;
    } catch (error) {
      console.error(`Broadcast error for maxUserId ${mu.maxUserId}:`, error);
    }
  }

  return NextResponse.json({ success: true, sent, total: maxUsers.length });
}
```

**Step 2: Commit**

```bash
git add src/app/api/admin/broadcast/
git commit -m "feat(bot): admin broadcast API for Max messenger"
```

---

### Task 12: Admin UI — Broadcast section in settings

**Files:**
- Modify: `src/app/admin/settings/page.tsx`

**Step 1: Add broadcast form**

Add a section in the Max notification settings area with a textarea and "Отправить всем" button. On submit, calls `POST /api/admin/broadcast`. Shows result (sent N of M).

**Step 2: Commit**

```bash
git add src/app/admin/settings/
git commit -m "feat(bot): broadcast UI in admin settings"
```

---

### Task 13: Docker setup for bot

**Files:**
- Create: `bot/Dockerfile`
- Modify: `docker-compose.yml`
- Modify: `deploy.sh`

**Step 1: Create bot/Dockerfile**

```dockerfile
FROM node:20-alpine
WORKDIR /app

COPY package.json ./
RUN npm install --no-audit --no-fund
COPY dist ./dist
COPY ../prisma ./prisma
RUN npx prisma generate

USER node
CMD ["node", "dist/index.js"]
```

**Step 2: Add bot service to docker-compose.yml**

```yaml
  bot:
    build:
      context: .
      dockerfile: bot/Dockerfile
    container_name: csm-bot
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - ./data:/app/data
    mem_limit: 128m
    depends_on:
      - app
    networks:
      - app-network
```

**Step 3: Update deploy.sh to build bot**

Add after Next.js build step:

```bash
echo "[2.5/5] Building Max bot..."
cd bot && npm install --no-audit --no-fund && npx tsc && cd ..
```

**Step 4: Commit**

```bash
git add bot/Dockerfile docker-compose.yml deploy.sh
git commit -m "feat(bot): Docker setup for Max bot container"
```

---

### Task 14: Prisma sharing between bot and app

**Files:**
- Modify: `bot/Dockerfile`
- Modify: `bot/package.json`

The bot needs access to the same Prisma schema. The Dockerfile copies `prisma/` from the project root and runs `npx prisma generate`. The `bot/package.json` already includes `@prisma/client`. The `DATABASE_URL` env var in docker-compose points to the same SQLite file via shared volume.

**Step 1: Fix bot Dockerfile to properly copy prisma schema**

```dockerfile
FROM node:20-alpine
WORKDIR /app

COPY bot/package.json ./
RUN npm install --no-audit --no-fund
COPY bot/dist ./dist
COPY prisma ./prisma
RUN npx prisma generate

USER node
CMD ["node", "dist/index.js"]
```

Note: build context is project root (`.`), not `bot/`.

**Step 2: Update docker-compose bot build context**

```yaml
  bot:
    build:
      context: .
      dockerfile: bot/Dockerfile
```

**Step 3: Commit**

```bash
git add bot/Dockerfile docker-compose.yml
git commit -m "fix(bot): correct Prisma schema sharing in Docker"
```
